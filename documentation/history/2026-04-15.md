# 2026-04-15

## Accomplished

- Migrated from Nx/npm/Jest/esbuild monorepo to the bun-based plugin template layout.
- Moved source from `apps/plugin/src/` → `src/` (via `git mv` to preserve history).
- Renamed `src/assets/styles.css` → `src/styles.src.css` (Tailwind v4 source).
- Replaced tooling: `package.json`, `tsconfig.json`, ESLint (flat config), Prettier, commitlint, Husky, scripts (`scripts/build.ts`, `scripts/update-version.ts`, `scripts/version-bump.ts`, `scripts/generate-changelog.ts`, `scripts/create-release-zip.ts`, `scripts/release.sh`, `scripts/calculate-next-version.ts`), GitHub workflows (`.github/workflows/ci.yml`, `.github/workflows/release.yml`).
- Deleted Nx/Jest infrastructure (`nx.json`, `jest.config.ts`, `jest.preset.js`, `version-bump.mjs`, `package-lock.json`, `tools/`, `libs/`, `apps/`, `.nx/`, `.eslintrc.json`, `.eslintignore`, `.nxignore`, `.npmrc`, `.prettierrc`, `.prettierignore`, `.lintstagedrc.js`).
- Kept pre-existing `docs/` (Jekyll user guide) and completed it with `docs/tips.md` and `docs/release-notes.md`.
- Created `documentation/` (technical docs): `Architecture.md`, `Business Rules.md`, `Configuration.md`, `Domain Model.md`, and this history entry.
- New `AGENTS.md` aligned with the template; `CLAUDE.md` references it via `@./AGENTS.md` (kept from prior session if present).
- Created a terse `CHANGELOG.md`.

## Decisions

- Kept semver continuity (version stays at `1.0.3`). No reset.
- Preserved runtime deps: `replicate@0.32.0`, `node-fetch@3.3.2`, `immer@10.0.4`, `zod@3.23.8`, `tslib@2.4.0`. These were the versions present in the old `package.json`.
- Preserved plugin id `replicate` and command id `generate-image-using-replicate` (BR-007).
- Kept `isDesktopOnly: true` — `node-fetch` remains the fetch polyfill (BR-005).
- Did not refactor source code logic — only the directory path changed. Import paths inside `src/` were already relative and required no fixups.

## Open questions / manual follow-ups

- Runtime verification in a real vault: generate-image flow (selection path + modal path), clipboard toggle, append-to-note toggle, invalid-JSON configuration handling in the settings tab.
- Verify that `src/styles.src.css` (moved verbatim from the old `assets/styles.css`) compiles correctly through the Tailwind v4 CLI pipeline. The old file contained plain CSS with class selectors; it needs a `@import "tailwindcss"` directive if Tailwind utilities should work on future additions.
- Verify that the release workflow (`.github/workflows/release.yml`) triggers on the next version tag push and produces `main.js`, `manifest.json`, `styles.css` assets named correctly (tag = raw version, no `v` prefix).
- The `zod` dependency is currently unused in the source. Consider dropping it or adding schema validation for `imageGenerationConfiguration`.
- `src/assets/buy-me-a-coffee.png` is still referenced via a hard-coded GitHub raw URL pointing at the old template repo path (`obsidian-plugin-template/raw/main/apps/plugin/src/assets/buy-me-a-coffee.png`). Update to a correct URL for this plugin or bundle the image properly.
